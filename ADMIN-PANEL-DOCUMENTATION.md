# 📊 نظام لوحة التحكم - Admin Panel Documentation

## نظرة عامة على المشروع

تم تطوير **نظام لوحة تحكم كامل** لإدارة متجر إلكتروني متكامل، يوفر للمسؤولين (Admins) تحكمًا كاملاً في جميع جوانب المتجر من منتجات وطلبات وإعدادات وتخصيصات.

---

## 🔐 نظام الصلاحيات والوصول

### آلية التحقق من صلاحيات الأدمن

تم تطوير نظام محكم للتحقق من صلاحيات المستخدم:

1. **جدول `admins` في قاعدة البيانات**:
   - يحتوي على `user_id` (FK → auth.users)
   - `role`: دور المستخدم (admin/super_admin)
   - `is_active`: حالة تفعيل الحساب

2. **AuthContext**:
   - يتحقق تلقائيًا من وجود المستخدم في جدول admins
   - يوفر `isAdmin` boolean في جميع أنحاء التطبيق
   - يتم التحقق في كل صفحة admin عبر `useAuth()`

3. **الوصول للوحة التحكم**:
   - يظهر رابط "لوحة التحكم" في قائمة الحساب للمستخدمين المسجلين كـ admins فقط
   - تحويل تلقائي للمستخدمين غير المصرح لهم

---

## 📱 الصفحات والوظائف

### 1. 🏠 **الصفحة الرئيسية - Dashboard** (`/admin`)

**الوظيفة الأساسية:**
صفحة نظرة عامة تعرض إحصائيات شاملة عن أداء المتجر في الوقت الفعلي.

**المميزات:**

#### 📊 بطاقات الإحصائيات (Stats Cards)
- **إجمالي الإيرادات**:
  - حساب تلقائي من جميع الطلبات
  - عرض النمو (+5.4%)
  - تنسيق بالعملة العربية

- **إجمالي الطلبات**:
  - عدد كل الطلبات في النظام
  - معدل النمو (+12%)

- **إجمالي المنتجات**:
  - عدد المنتجات النشطة
  - معدل النمو (+2.1%)

- **الطلبات المعلقة**:
  - الطلبات في حالة "processing"
  - معدل التغيير (-3%)

#### 📋 جدول آخر الطلبات
- عرض آخر 5 طلبات
- معلومات كل طلب:
  - رقم الطلب (معرف مختصر)
  - اسم العميل
  - المبلغ الإجمالي
  - حالة الطلب (badges ملونة)
  - تاريخ الطلب
  - زر "عرض التفاصيل"

**التقنيات المستخدمة:**
- Supabase Realtime Queries
- React Hooks (useState, useEffect)
- Dynamic Styling with Tailwind CSS
- Responsive Grid System

---

### 2. 📦 **إدارة الطلبات - Orders Management** (`/admin/orders`)

**الوظيفة الأساسية:**
إدارة شاملة لجميع طلبات المتجر مع إمكانية تحديث الحالات.

**المميزات:**
- عرض جميع الطلبات في جدول تفاعلي
- فلترة الطلبات حسب الحالة:
  - `processing` - قيد التنفيذ
  - `delivered` - مكتمل
  - `cancelled` - ملغي
- تحديث حالة الطلب من القائمة مباشرة
- عرض تفاصيل كل طلب:
  - معلومات العميل الكاملة
  - عنوان التوصيل
  - المنتجات المطلوبة
  - طريقة الدفع
  - إجمالي المبلغ

**العمليات المتاحة:**
- ✅ تأكيد الطلب
- 🚚 تحديث حالة الشحن
- ❌ إلغاء الطلب
- 📧 إرسال إشعار للعميل

---

### 3. 🛍️ **إدارة المنتجات - Products Management** (`/admin/products`)

**الوظيفة الأساسية:**
التحكم الكامل في كتالوج المنتجات (إضافة، تعديل، حذف).

**المميزات:**

#### إضافة منتج جديد
- **معلومات أساسية**:
  - اسم المنتج (title)
  - الوصف (description)
  - السعر الحالي (price)
  - السعر القديم (old_price) - للتخفيضات
  - الكمية المتوفرة (stock)

- **تصنيف وتنظيم**:
  - اختيار القسم (category_id)
  - Slug للـ URL
  - منتج مميز (is_featured)
  - حالة النشر (is_active)

- **صور المنتج**:
  - رفع صورة رئيسية (image_url)
  - رفع معرض صور متعدد (images array)
  - معاينة قبل الرفع

- **خيارات إضافية**:
  - المقاسات المتاحة (sizes)
  - الألوان المتاحة (colors)

#### تعديل المنتجات
- تعديل جميع بيانات المنتج
- تحديث الصور
- تغيير حالة المخزون

#### حذف المنتجات
- حذف آمن مع تأكيد
- التحقق من الطلبات المرتبطة

**التقنيات:**
- Form Validation
- Image Upload & Preview
- Multi-select Components
- Rich Text Editor للوصف

---

### 4. 📑 **إدارة الأقسام - Categories Management** (`/admin/categories`)

**الوظيفة الأساسية:**
إنشاء وإدارة أقسام المنتجات (رجالي، حريمي، أطفالي، إلخ).

**المميزات:**
- **إضافة قسم جديد**:
  - اسم القسم (name)
  - وصف القسم (description)
  - Slug للـ URL
  - ترتيب العرض (display_order)
  - حالة النشر (is_active)

- **تعديل الأقسام**:
  - تحديث البيانات
  - إعادة ترتيب الأقسام
  - تفعيل/إلغاء تفعيل

- **عرض إحصائيات**:
  - عدد المنتجات لكل قسم
  - الأقسام الأكثر مبيعًا

---

### 5. 🎫 **إدارة الكوبونات - Coupons Management** (`/admin/coupons`)

**الوظيفة الأساسية:**
إنشاء وإدارة كوبونات الخصم لتشجيع المبيعات.

**المميزات:**

#### إنشاء كوبون جديد
- **معلومات الكوبون**:
  - كود الكوبون (code) - فريد
  - نوع الخصم (discount_type):
    - نسبة مئوية (percentage)
    - قيمة ثابتة (fixed)
  - قيمة الخصم (discount_value)

- **قيود الاستخدام**:
  - تاريخ البداية (valid_from)
  - تاريخ الانتهاء (valid_until)
  - الحد الأدنى للطلب (min_order_value)
  - عدد مرات الاستخدام الأقصى (max_uses)
  - حالة التفعيل (is_active)

#### تتبع الكوبونات
- عدد المرات المستخدمة
- الإيرادات المتأثرة
- المستخدمون الذين استخدموا الكوبون

---

### 6. 🖼️ **إدارة السلايدر - Slider Management** (`/admin/slider`)

**الوظيفة الأساسية:**
إدارة صور السلايدر الرئيسي في الصفحة الرئيسية للمتجر.

**المميزات:**
- **إضافة صورة جديدة**:
  - رفع صورة عالية الجودة
  - عنوان العرض (title)
  - وصف العرض (description)
  - ترتيب العرض (display_order)
  - حالة النشر (is_active)

- **تعديل الصور**:
  - تحديث المحتوى
  - إعادة ترتيب الصور (drag & drop)
  - تفعيل/إلغاء تفعيل

- **معاينة**:
  - رؤية كيف ستظهر الصورة في الموقع

**الاستخدام:**
- عروض خاصة
- منتجات جديدة
- خصومات موسمية
- حملات تسويقية

---

### 7. 📢 **رسائل الشريط العلوي - Top Bar Messages** (`/admin/top-bar-messages`)

**الوظيفة الأساسية:**
إدارة الرسائل المتحركة في الشريط الأحمر أعلى الموقع.

**المميزات:**
- **إضافة رسالة**:
  - نص الرسالة (message)
  - ترتيب الظهور (display_order)
  - حالة النشر (is_active)

- **عرض تلقائي**:
  - التبديل التلقائي بين الرسائل
  - رسائل متعددة نشطة

**أمثلة الاستخدام:**
- "شحن مجاني للطلبات فوق 500 جنيه"
- "خصم 25% على جميع المنتجات الجديدة"
- "توصيل سريع لجميع المحافظات"

---

### 8. 🎨 **إعدادات الألوان - Theme Settings** (`/admin/theme-settings`)

**الوظيفة الأساسية:**
تخصيص ألوان الموقع بالكامل من لوحة التحكم.

**الألوان القابلة للتخصيص:**

1. **اللون الأساسي (Primary Color)**:
   - الأزرار الرئيسية
   - الروابط
   - العناصر التفاعلية

2. **لون النص على الأزرار (Button Text)**:
   - نص الأزرار الأساسية

3. **لون السعر (Price Color)**:
   - عرض أسعار المنتجات

4. **لون الشريط العلوي (Top Bar)**:
   - خلفية الشريط العلوي

5. **نص الشريط العلوي (Top Bar Text)**:
   - نص رسائل الشريط

6. **خلفية كروت المنتجات (Product Card BG)**:
   - خلفية بطاقات عرض المنتجات

**المميزات:**
- **Color Picker** تفاعلي لاختيار الألوان
- معاينة فورية للتغييرات
- حفظ كـ CSS Variables
- تطبيق تلقائي على كامل الموقع
- دعم Dark Mode

**التقنية:**
- CSS Custom Properties (Variables)
- Real-time Color Preview
- Supabase للتخزين

---

### 9. 🦶 **إعدادات الفوتر - Footer Settings** (`/admin/footer-settings`)

**الوظيفة الأساسية:**
التحكم الكامل في محتوى وروابط الفوتر (Footer).

**الأقسام القابلة للتعديل:**

#### معلومات المتجر
- اسم المتجر (store_name)
- وصف المتجر (store_description)
- شعار المتجر (store_logo)

#### روابط التواصل الاجتماعي
- **Facebook** (facebook_url)
- **Twitter/X** (twitter_url)
- **Instagram** (instagram_url)
- **YouTube** (youtube_url)
- **TikTok** (tiktok_url)
- **LinkedIn** (linkedin_url)

#### معلومات الاتصال
- رقم الهاتف (phone)
- رقم واتساب (whatsapp)
- البريد الإلكتروني (email)
- العنوان (address)

**المميزات:**
- رموز (icons) تلقائية لكل منصة
- تحقق من صحة الروابط
- واتساب click-to-chat مباشر
- حالة التفعيل (is_active)

---

## 🛠️ التقنيات المستخدمة

### Frontend
- **Next.js 14** (App Router)
- **React 18** (Server & Client Components)
- **TypeScript** (Type Safety)
- **Tailwind CSS** (Styling)
- **Headless UI** (UI Components)

### Backend & Database
- **Supabase** (PostgreSQL)
- **Row Level Security (RLS)** للأمان
- **Supabase Auth** (المصادقة)
- **Supabase Storage** (تخزين الصور)

### State Management
- **React Context API**:
  - AuthContext (المستخدم والصلاحيات)
  - CartContext (سلة المشتريات)
  - WishlistContext (المفضلة)

### أمان وصلاحيات
- **Server-Side Authentication Check**
- **Protected Routes** (Admin Only)
- **RBAC** (Role-Based Access Control)
- **SQL Injection Protection**
- **XSS Protection**

---

## 📐 معمارية النظام (Architecture)

### هيكل المجلدات
```
src/
├── app/
│   ├── admin/
│   │   ├── layout.tsx           # Admin Layout with Sidebar
│   │   ├── page.tsx             # Dashboard
│   │   ├── orders/
│   │   ├── products/
│   │   ├── categories/
│   │   ├── coupons/
│   │   ├── slider/
│   │   ├── top-bar-messages/
│   │   ├── theme-settings/
│   │   └── footer-settings/
│   └── ...
├── components/
│   ├── AdminSidebar.tsx         # Sidebar Navigation
│   └── ...
├── contexts/
│   ├── AuthContext.tsx          # Authentication
│   └── ...
└── lib/
    └── supabaseClient.ts        # Supabase Config
```

### نموذج قاعدة البيانات (Database Schema)

#### جدول `admins`
```sql
CREATE TABLE admins (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES auth.users(id) UNIQUE,
  role VARCHAR(50) DEFAULT 'admin',
  is_active BOOLEAN DEFAULT true,
  created_at TIMESTAMP DEFAULT NOW()
);
```

#### جدول `products`
```sql
CREATE TABLE products (
  id UUID PRIMARY KEY,
  title VARCHAR(255),
  slug VARCHAR(255) UNIQUE,
  description TEXT,
  price DECIMAL(10,2),
  old_price DECIMAL(10,2),
  category_id UUID REFERENCES categories(id),
  image_url TEXT,
  images TEXT[],
  stock INTEGER,
  sizes TEXT[],
  colors JSONB,
  is_featured BOOLEAN DEFAULT false,
  is_active BOOLEAN DEFAULT true,
  created_at TIMESTAMP DEFAULT NOW()
);
```

#### جدول `orders`
```sql
CREATE TABLE orders (
  id UUID PRIMARY KEY,
  user_id UUID REFERENCES auth.users(id),
  customer_name VARCHAR(255),
  customer_email VARCHAR(255),
  customer_phone VARCHAR(50),
  shipping_address TEXT,
  total_amount DECIMAL(10,2),
  order_status VARCHAR(50) DEFAULT 'processing',
  payment_method VARCHAR(50),
  created_at TIMESTAMP DEFAULT NOW()
);
```

---

## 🔒 الأمان والصلاحيات (Security)

### Row Level Security (RLS) Policies

#### سياسة الـ admins
```sql
-- السماح للـ admins فقط بالقراءة والكتابة
CREATE POLICY admin_access ON admins
  FOR ALL
  USING (
    auth.uid() IN (SELECT user_id FROM admins WHERE is_active = true)
  );
```

#### سياسة المنتجات
```sql
-- الجميع يمكنهم القراءة
CREATE POLICY products_read ON products
  FOR SELECT
  USING (is_active = true);

-- الـ admins فقط يمكنهم التعديل
CREATE POLICY products_write ON products
  FOR ALL
  USING (
    auth.uid() IN (SELECT user_id FROM admins WHERE is_active = true)
  );
```

### حماية الصفحات
```typescript
// في كل صفحة admin
const { user, isAdmin } = useAuth();

useEffect(() => {
  if (!user || !isAdmin) {
    router.push('/login');
  }
}, [user, isAdmin]);
```

---

## 📱 Responsive Design

### Desktop (> 1024px)
- Sidebar ثابت على اليمين
- عرض كامل للجداول
- 4 أعمدة للبطاقات الإحصائية

### Tablet (768px - 1024px)
- Sidebar قابل للطي
- 2 أعمدة للبطاقات
- جداول قابلة للتمرير أفقيًا

### Mobile (< 768px)
- قائمة أفقية scrollable في الأعلى
- عمود واحد للبطاقات
- جداول responsive مع scroll

---

## 🎯 مميزات إضافية

### 1. البحث والفلترة
- بحث في المنتجات (بالاسم والوصف)
- فلترة الطلبات حسب الحالة
- فلترة حسب التاريخ

### 2. الإشعارات
- إشعار عند طلب جديد
- إشعار عند انخفاض المخزون
- إشعارات الأخطاء والنجاح

### 3. التحليلات
- إحصائيات المبيعات
- معدلات النمو
- المنتجات الأكثر مبيعًا
- تقارير الإيرادات

### 4. النسخ الاحتياطي
- تصدير البيانات (Export)
- استيراد البيانات (Import)
- تاريخ التعديلات

---

## 🚀 سير العمل (Workflow)

### إضافة منتج جديد
1. الدخول لـ `/admin/products`
2. الضغط على "إضافة منتج"
3. ملء البيانات المطلوبة
4. رفع الصور
5. اختيار القسم والخيارات
6. حفظ → المنتج يظهر مباشرة في المتجر

### إدارة طلب
1. إشعار بطلب جديد
2. الدخول لـ `/admin/orders`
3. فتح تفاصيل الطلب
4. مراجعة المنتجات والعنوان
5. تحديث الحالة (تأكيد → شحن → توصيل)
6. العميل يتلقى إشعار تلقائي

### تخصيص ألوان الموقع
1. الدخول لـ `/admin/theme-settings`
2. اختيار اللون من Color Picker
3. معاينة التغيير
4. حفظ → التطبيق الفوري على الموقع

---

## 📊 الإحصائيات والتقارير

### Dashboard الرئيسي يعرض:
- إجمالي الإيرادات (مع معدل النمو)
- عدد الطلبات (مع معدل النمو)
- عدد المنتجات (مع معدل النمو)
- الطلبات المعلقة (مع معدل التغيير)
- آخر 5 طلبات
- المنتجات قليلة المخزون

### التقارير المتاحة:
- تقرير المبيعات اليومي/الأسبوعي/الشهري
- أكثر المنتجات مبيعًا
- أكثر الأقسام مبيعًا
- معدل إتمام الطلبات

---

## 🎓 خلاصة المشروع

تم تطوير **نظام إدارة متكامل** يوفر:

✅ **تحكم كامل** في جميع جوانب المتجر
✅ **واجهة سهلة الاستخدام** ومتجاوبة
✅ **أمان عالي** مع RLS و Authentication
✅ **تخصيص شامل** للألوان والمحتوى
✅ **إحصائيات فورية** لمتابعة الأداء
✅ **إدارة فعالة** للمنتجات والطلبات

النظام جاهز للاستخدام في بيئة الإنتاج ويدعم التوسع المستقبلي.

---

## 📞 ملاحظات فنية للمناقشة

### نقاط القوة
1. **معمارية نظيفة**: فصل واضح بين Server و Client Components
2. **أداء عالي**: استخدام Next.js 14 App Router و Server Components
3. **أمان متقدم**: RLS Policies و Protected Routes
4. **UX ممتاز**: Responsive Design و Real-time Updates
5. **قابلية التوسع**: بنية modular وسهولة إضافة صفحات جديدة

### التحديات التي تم حلها
1. ✅ إصلاح مشاكل Suspense Boundaries في Next.js
2. ✅ تطبيق RLS Policies صحيحة
3. ✅ إدارة الصور والملفات
4. ✅ التحقق من الصلاحيات بشكل آمن
5. ✅ تخصيص الألوان ديناميكيًا

### التطويرات المستقبلية المقترحة
- 📊 Dashboard أكثر تفصيلاً مع Charts
- 📧 نظام إشعارات Email/SMS
- 📱 تطبيق موبايل للأدمن
- 🤖 AI لتحليل سلوك العملاء
- 📦 تكامل مع شركات الشحن

---

**تم التطوير بواسطة**: فريق التطوير
**التاريخ**: يناير 2026
**التقنيات**: Next.js 14, React 18, TypeScript, Supabase, Tailwind CSS
