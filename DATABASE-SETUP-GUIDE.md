# 📋 دليل إعداد قاعدة البيانات

هذا الدليل يشرح كيفية حذف البيانات القديمة وإضافة بيانات جديدة للمتجر.

---

## ⚠️ تحذير مهم

**هذه العمليات ستحذف جميع البيانات الموجودة!**
تأكد من أخذ نسخة احتياطية قبل المتابعة.

---

## 🚀 خطوات التنفيذ

### الخطوة 1️⃣: افتح Supabase SQL Editor

1. اذهب إلى [Supabase Dashboard](https://supabase.com/dashboard)
2. اختر مشروعك
3. من القائمة الجانبية، اختر **SQL Editor**

---

### الخطوة 2️⃣: حذف البيانات القديمة

افتح ملف **`clear-all-data.sql`** وانسخ محتواه
الصقه في SQL Editor واضغط **Run**

**ما سيتم حذفه:**
- ✅ جميع المنتجات
- ✅ جميع الفئات
- ✅ جميع صور السلايدر
- ✅ عناصر السلة والمفضلة

---

### الخطوة 3️⃣: إضافة الفئات والمنتجات الجديدة

افتح ملف **`seed-new-data.sql`** وانسخ محتواه
الصقه في SQL Editor واضغط **Run**

**ما سيتم إضافته:**
- ✅ 3 فئات رئيسية: رجالي، حريمي، أطفالي
- ✅ 30 منتج مع صور حقيقية:
  - 11 منتج رجالي (قمصان، بناطيل، تيشيرتات، جاكيتات)
  - 11 منتج حريمي (فساتين، بلوزات، بناطيل، جاكيتات)
  - 8 منتجات أطفالي (ملابس أولاد وبنات)

---

### الخطوة 4️⃣: إضافة صور السلايدر

افتح ملف **`seed-slider-images.sql`** وانسخ محتواه
الصقه في SQL Editor واضغط **Run**

**ما سيتم إضافته:**
- ✅ 4 صور سلايدر عالية الجودة:
  - صورة للقسم الرجالي
  - صورة للقسم الحريمي
  - صورة للقسم الأطفالي
  - صورة للتخفيضات

---

## ✅ التحقق من النجاح

بعد تنفيذ جميع الخطوات، تحقق من:

### 1. الفئات
```sql
SELECT * FROM categories ORDER BY id;
```
يجب أن يظهر 3 فئات: رجالي، حريمي، أطفالي

### 2. المنتجات
```sql
SELECT
    p.name,
    c.name as category_name,
    p.price,
    p.discount_price
FROM products p
LEFT JOIN categories c ON p.category_id = c.id
ORDER BY c.id, p.id;
```
يجب أن يظهر 30 منتج

### 3. صور السلايدر
```sql
SELECT * FROM slider_images ORDER BY order_index;
```
يجب أن يظهر 4 صور

---

## 🌐 مصادر الصور

جميع الصور مستخدمة من **Unsplash** (مجانية للاستخدام التجاري):
- 📸 صور عالية الجودة
- ✅ مجانية تماماً
- 🔗 روابط مباشرة CDN سريع

---

## 🔄 إعادة التشغيل

إذا أردت إعادة التشغيل من البداية:

1. شغّل `clear-all-data.sql` مرة أخرى
2. شغّل `seed-new-data.sql` مرة أخرى
3. شغّل `seed-slider-images.sql` مرة أخرى

---

## 📝 ملاحظات

### تعديل البيانات
يمكنك تعديل أي بيانات في ملفات SQL قبل التشغيل:
- الأسعار
- الأوصاف
- الصور (استبدل روابط Unsplash بروابط صورك)
- الكميات المتاحة

### إضافة المزيد من المنتجات
افتح ملف `seed-new-data.sql` وأضف منتجات جديدة باستخدام نفس التنسيق:

```sql
INSERT INTO products (name, description, price, discount_price, category_id, image_url, stock, is_active, created_at) VALUES
('اسم المنتج', 'الوصف', 299.00, 249.00,
 (SELECT id FROM categories WHERE slug = 'men'),
 'https://images.unsplash.com/photo-xxx?w=500', 50, true, NOW());
```

---

## ⚡ نصائح

1. **شغّل الملفات بالترتيب** (clear → seed-new-data → seed-slider-images)
2. **تحقق من النتائج** بعد كل خطوة
3. **أخذ backup** قبل الحذف
4. **تحديث الصور** بصور خاصة بمنتجاتك لاحقاً

---

## 🆘 حل المشاكل

### خطأ: Foreign key constraint
**السبب:** محاولة حذف فئة مرتبطة بمنتجات
**الحل:** احذف المنتجات أولاً، ثم الفئات

### خطأ: Duplicate key
**السبب:** البيانات موجودة مسبقاً
**الحل:** شغّل `clear-all-data.sql` أولاً

### الصور لا تظهر
**السبب:** مشكلة في رابط الصورة
**الحل:** تحقق من أن روابط Unsplash صحيحة

---

## 📞 الدعم

إذا واجهت أي مشكلة، تحقق من:
- Console في المتصفح (F12)
- Logs في Supabase
- هيكل الجداول في قاعدة البيانات

---

✅ **بالتوفيق!** 🎉
