-- ============================================
-- 🗑️ حذف جميع البيانات القديمة
-- ============================================
-- تحذير: هذا سيحذف جميع المنتجات والفئات والصور!
-- ============================================

-- 1️⃣ حذف جميع المنتجات
-- ============================================
DELETE FROM products;

-- إعادة تعيين تسلسل الـ IDs (اختياري)
-- ALTER SEQUENCE products_id_seq RESTART WITH 1;

-- 2️⃣ حذف جميع الفئات
-- ============================================
DELETE FROM categories;

-- إعادة تعيين تسلسل الـ IDs (اختياري)
-- ALTER SEQUENCE categories_id_seq RESTART WITH 1;

-- 3️⃣ حذف جميع صور السلايدر
-- ============================================
DELETE FROM slider_images;

-- إعادة تعيين تسلسل الـ IDs (اختياري)
-- ALTER SEQUENCE slider_images_id_seq RESTART WITH 1;

-- 4️⃣ حذف بيانات إضافية (إذا كانت موجودة)
-- ============================================
-- حذف العناصر من السلة
DELETE FROM cart_items;

-- حذف العناصر من المفضلة
DELETE FROM wishlist_items;

-- حذف الطلبات القديمة (اختياري - احذر!)
-- DELETE FROM order_items;
-- DELETE FROM orders;

-- ============================================
-- ✅ تم حذف جميع البيانات!
-- ============================================
-- الآن يمكنك تشغيل ملف seed-new-data.sql لإضافة البيانات الجديدة
-- ============================================
