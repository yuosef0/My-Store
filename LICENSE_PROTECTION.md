# 🔐 نظام حماية الـ Source Code

هذا المشروع محمي بنظام License Protection لمنع النسخ أو الـ deployment غير المصرح به.

## 📋 كيف يعمل النظام؟

النظام يتحقق من:
1. **الدومين**: يسمح فقط بالدومينات المسجلة
2. **License Key**: يتحقق من وجود مفتاح صالح
3. **Deployment Tracking**: يسجل معلومات كل deployment ويرسل تنبيهات في حالة deployment غير مصرح به

## 🚀 الإعداد للمرة الأولى

### 1. توليد License Key

```bash
npm run generate-license
```

سينتج هذا مفتاح عشوائي - انسخه.

### 2. إضافة المفاتيح للـ `.env`

أضف هذه السطور لملف `.env`:

```env
NEXT_PUBLIC_LICENSE_KEY=المفتاح_الذي_تم_توليده
NEXT_PUBLIC_OWNER_EMAIL=your-email@example.com
```

### 3. إضافة المفاتيح لـ Vercel

اذهب إلى Vercel Dashboard → Settings → Environment Variables وأضف:

- `NEXT_PUBLIC_LICENSE_KEY`: المفتاح الذي تم توليده
- `NEXT_PUBLIC_OWNER_EMAIL`: بريدك الإلكتروني

### 4. تحديث الدومينات المسموح بها

في ملف `src/lib/license.ts`، حدث قائمة `ALLOWED_DOMAINS`:

```typescript
const ALLOWED_DOMAINS = [
  'localhost',
  '127.0.0.1',
  'my-store-seven-self.vercel.app',  // دومين production
  'my-custom-domain.com',             // دومين مخصص (اختياري)
];
```

## 🔔 تفعيل التنبيهات (اختياري)

لتلقي تنبيهات عند محاولة deployment غير مصرح به:

### استخدام Discord Webhook:

1. اذهب إلى Discord Server Settings → Integrations → Webhooks
2. أنشئ Webhook جديد وانسخ الـ URL
3. أضفه للـ `.env`:

```env
NEXT_PUBLIC_ALERT_WEBHOOK_URL=https://discord.com/api/webhooks/xxxxx/xxxxx
```

## ⚠️ ماذا يحدث عند Deployment غير مصرح به؟

إذا حاول شخص عمل deploy للكود على دومين غير مصرح:

1. ✋ **يتم حجب الموقع**: تظهر رسالة "Unauthorized Deployment"
2. 📝 **تسجيل المعلومات**: يتم تسجيل hostname, timestamp, user agent
3. 🔔 **إرسال تنبيه**: إذا كان webhook مفعل، سيتم إرسال تنبيه فوري
4. 🚨 **رسائل في Console**: تظهر رسائل تحذيرية في browser console

## 🛡️ مستويات الحماية

### المستوى 1: Domain Whitelisting ✓
- يمنع التشغيل على دومينات غير مصرح بها
- صعب التجاوز بدون تعديل الكود

### المستوى 2: License Key Validation ✓
- يتطلب مفتاح صالح في environment variables
- حتى لو تم نسخ الكود، لن يعمل بدون المفتاح

### المستوى 3: Deployment Tracking ✓
- يرسل معلومات عن كل deployment
- يتيح لك معرفة من يستخدم الكود

## 🔧 للمطورين المصرح لهم

### إضافة دومين جديد مصرح به:

1. افتح `src/lib/license.ts`
2. أضف الدومين الجديد لـ `ALLOWED_DOMAINS`
3. Deploy التحديث

### تعطيل الحماية مؤقتاً (Development):

الحماية معطلة تلقائياً في development mode (`NODE_ENV=development`)

## ⚙️ استكشاف الأخطاء

### المشكلة: "Unauthorized Deployment" على دومين صحيح

**الحل:**
1. تأكد من إضافة الدومين لـ `ALLOWED_DOMAINS`
2. تأكد من وجود `NEXT_PUBLIC_LICENSE_KEY` في environment variables
3. أعد build و deploy

### المشكلة: لا تصل التنبيهات

**الحل:**
1. تأكد من صحة webhook URL
2. تأكد من إضافته لـ environment variables في Vercel
3. تحقق من صلاحيات الـ webhook

## 📞 الدعم

إذا واجهت أي مشاكل مع نظام الحماية، تواصل معنا.

---

**ملاحظة مهمة:** لا تشارك الـ License Key في أي مكان عام (GitHub, Discord, etc.)
